services:
  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: uimm
      MYSQL_DATABASE: viviers_prod
    volumes:
      - db_data:/var/lib/mysql

  adminer:
    image: adminer
    ports:
      - "8081:8080"
    depends_on:
      - db

volumes:
  db_data: